import React from 'react';

type StatsProps = {
    principal: any;
};

// eslint-disable-next-line @typescript-eslint/no-unused-vars
const Stats = ({ principal }: StatsProps) => {
    return (
        <>
            <div>STATS COMPONENT</div>
        </>
    );
};

export default Stats;
